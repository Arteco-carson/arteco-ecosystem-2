# --- SECURITY & SECRETS (Critical) ---
.env
.env.local
.env.*.local
*.pem
*.key
# Ignore the Gemini CLI config/cache folder to protect your API key
.gemini/

# --- NODE / JAVASCRIPT ---
node_modules/
npm-debug.log*
yarn-error.log*
yarn-debug.log*
dist/
build/
# Vite build timestamp (common in modern React)
*.timestamp-*

# --- REACT NATIVE / EXPO (Added based on your Tech Stack) ---
.expo/
*.jks
*.keystore
*.p12
android/app/debug.keystore
web-build/

# --- VISUAL STUDIO / .NET ---
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/
# Ignore all appsettings except the base one (safer for dev/prod secrets)
appsettings.*.json
!appsettings.json

# --- VS CODE ---
.vscode/
*.code-workspace

# --- OS & LOGS ---
Thumbs.db
*.swp
ehthumbs.db
.DS_Store
*.log